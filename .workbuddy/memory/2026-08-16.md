# 2026-08-16 工作日志

## 交接班页面原型（语音确认 + 竖排标题 + 错误闪烁）

### 背景
用户要求重做交接班页面：
- 每个大项标题**竖排在最右边**（不是顶部横排）
- 不要"当前项/共几个"数字
- 选项有**单选/多选**两种
- 正常状态文字**淡黑色**，错误/不相符时**琥珀色缓慢闪烁**
- 每条下面有**蓝色小字语音确认语句**（如"现在是相关进近独立离场，1类盲降，用19R，19L落地"）
- 用户**按住**这句话 → 软件开始录音 → 对比是否按这句构成念出

### 问题
1. `D:\GitHub\e_duty_handbook\UI\设计\交接班页面.js` 是**空文件**（0 字节），用户说写了内容但实际没有
2. 当前对话模型**不支持图片**，看不到用户提到的交接班本截图

### 处理
基于文字描述做独立原型 `交接班页面-原型.html`，数据用合理示例：
- 5 个分组（盲降设备/跑道/天气/设备/军航），第 2 个分组（跑道）默认 error 状态演示琥珀色闪烁
- 竖排标题用 `writing-mode: vertical-rl + text-orientation: upright`
- 单选 chip 互斥，多选 chip 可叠加
- 蓝色小字语音语句 + 🎤 图标 + "按住录音"提示
- mousedown/touchstart 触发 recording 状态（脉冲动画），1.2s 后模拟识别结果（match 绿/mismatch 红）
- 不相符时自动把分组标记为 error 触发琥珀色闪烁
- 顶部有"模拟全部错误"按钮

### v2 处理（用户反馈"太丑 + 集成回 iPad 竖屏 + 真实数据"）
- 用户反馈：独立原型太丑、配色乱；要求**集成回 iPad Pro 竖屏整体设计**（index.html），保留悬浮框/水印/iPad 外框
- 用户真实数据已写入 `交接班页面.js`（12 个大分类：运行模式/设备/场面状况/天气/当前进近方式/空军活动/通航/特殊/流量控制/航行通告/动态）
- 数据结构：`{title, comp:[{type:'single'|'multi'|'text', items:[...]}]}`
  - single 单选 / multi 多选 / text：字符串=标签行（配"确认无误/不符"），数字数组=数字单选（如道面状况代码 0-5）
- 集成改动（index.html）：
  - view-handover 的容器 id 改 handoverBody，用 HANDOVER_GROUPS 数据渲染
  - .hg 分组卡片（白底圆角）：hg-body（选项）+ hg-title（竖排标题最右，writing-mode vertical-rl，深灰底 #334155 白字）
  - 删除旧 .v3-* 死代码 CSS（人员界面）
  - chip 单选互斥/多选叠加；text 数字渲染数字 chip
  - voice-sentence 蓝色小字语句：动态拼接各选项组选中项（"现在是XXX，XXX"），按住录音（1.5s 模拟识别），不相符 → 分组变琥珀色闪烁（@keyframes amberBlink 1.6s）
  - 删除旧的独立原型文件 交接班页面-原型.html
- 文档 4.3 章节重写（竖排标题 + 单选/多选 + 语音确认 + 颜色状态表）

### v3 处理（用户要求 React 项目重构）
- 用户反馈：
  1. 交接班标题在**左侧**、**无底色**、正常文字颜色（不要右侧深灰底白字）
  2. 只有大项里小项情况特殊才有对应颜色（错误琥珀色）
  3. 用 **ReactJS + Vite + TailwindCSS + Zustand** 重做整个项目
  4. 项目放 UI 目录下
- 创建 `D:\GitHub\e_duty_handbook\UI\handover-app\`：
  - 配置：package.json（react18/zustand4/tailwind3.4/vite5）、vite.config.js、tailwind.config.js（自定义 brand/amber/ink 色、amberBlink/recPulse 动画）、postcss.config.js、.npmrc（npmmirror，沙箱挡全局 .npmrc）
  - 数据：src/data/seats.js（席位+TABS+水印映射）、templates.js（附录 ABC）、handover.js（交接班 12 分类）
  - store：src/store/useAppStore.js（currentView/snap/currentSeat/pendingSeatId/handover 状态；toggleChip 单选互斥多选叠加/toggleConfirm/setGroupError/buildSentence）
  - 组件：App/IpadFrame(data-ipad)/StatusBar(实时时钟)/Watermark(z60+pointer-events-none)/Floats(贴边吸附+拖拽)/SeatView(纯选择器)/CheckListView(3列模板网格)/HandoverView(交接班)/SeatConfirmModal(确认不跳转)
  - HandoverView 细节：标题竖排最左无底色、single/multi/text 三种 chip、VoiceSentence 按住录音(1.5s 模拟识别 ok/fail，fail→分组琥珀色闪烁)
- 依赖安装：npm install 成功（132 包，npmmirror）；npm run build 成功（56 模块）
- dev server: http://localhost:5173 运行正常

### 经验沉淀
- 沙箱会拦截 `npm config set registry` 写全局 .npmrc → 改用**项目级 .npmrc** 指定镜像
- npm install 要后台跑 + TaskOutput 等待，避免超时
- UI 目录下发现用户新放的 docx（管制室机场席位交接班检查单 90 张纸等），尚未解析

### v4 修正（用户反馈"标题在左无底色 + 竖排兼容"）
- 用户反馈（针对 HTML 版 index.html）：
  1. 标题移到**左侧**、**无底色**、正常文字颜色（之前是右侧深灰底白字）
  2. 只有大项内小项特殊情况才有颜色（错误琥珀色），标题始终正常色
  3. "为什么有些标题不是垂直的"——flex 容器 + writing-mode 组合在部分浏览器下可能导致标题不竖排
- 改动：
  - HTML 版 .hg-title：去掉 background:#334155 深灰底 + 白字 → transparent + color:var(--text-2)；去掉 display:flex/align-items/justify-content（flex+writing-mode 兼容问题根源）→ 纯 writing-mode:vertical-rl + text-align:center + line-height:1；border-right 虚线分隔；位置移到 hg-body 前面（左侧）
  - 删除 .hg.error .hg-title 琥珀底规则（标题错误时保持正常色）
  - React 版 HandoverView.jsx 同样修复：标题去 flex 类、移到内容前（左侧）、无底色
  - npm run build 验证通过（注意：build 时 vite 清 dist 被沙箱 safe-delete 拦截，需先手动 rm -rf dist）

### 经验沉淀
- **竖排标题不要用 flex 容器**：writing-mode:vertical-rl 直接作用于文本节点即可，flex+writing-mode 组合在不同浏览器（尤其 Safari/iPad）可能不竖排；用 text-align:center + line-height 控制居中
- vite build 清空 dist 会被 WorkBuddy 沙箱的 safe-delete 拦截 → 先 `rm -rf dist` 再 build
- 用户"有些标题不垂直"的反馈，根因多为 CSS 兼容性，优先去掉可能引发差异的组合写法（flex+writing-mode）

### v4.1 修正（用户反馈"空分类标题显示 + 竖排方向 + 对齐"）
- 用户反馈：
  1. **空分类（comp 为空的 9 个大类）标题无法正确竖排展示**——卡片高度太矮，长标题（如"当前进近方式"6字）被 overflow:hidden 裁剪 → 之前"有些标题不垂直"的真根因其实是裁剪，不是 flex
  2. 标题要**从左到右的垂直**（vertical-lr），不是古书式从右往左（vertical-rl）
  3. 所有分类标题**没对齐** → 需要统一最低高度 + 居中
- 改动：
  - .hg-title：writing-mode vertical-rl → **vertical-lr**；加 `min-height:170px` + `display:grid; place-items:center`（竖排文字在容器中水平垂直居中）
  - min-height 170px 计算：最长标题"当前进近方式"6字 × (14px+8px 字距) + 上下 padding 28px ≈ 160px，留余量 170
  - React 版同步：.writing-vertical 改 vertical-lr；标题内联 style minHeight:170 + grid 居中
  - npm run build 通过

### 经验沉淀
- **竖排标题要设统一 min-height + grid place-items:center**：否则空内容卡片高度不足时，长标题竖排被 overflow 裁剪，看起来像"不垂直/坏掉"
- 中文竖排方向：vertical-rl=古书从右往左，vertical-lr=从左到右；用户要 lr
- 标题居中最可靠方案：writing-mode + display:grid + place-items:center（flex+writing-mode 的组合不可靠）

### v4.2 修正（用户反馈"标题高度还是不够，要完全展示"）
- 用户反馈：170px 还不够，标题（尤其"当前进近方式"6字）仍可能被裁剪
- 改动（HTML + React 同步）：
  - min-height: 170px → **200px**（最坏情况 6字×15px + 字距 + padding ≈ 176px，留 24px 余量）
  - letter-spacing: 8px → **4px**（降低高度占用，字距紧凑更优美）
  - 字号 14px → **15px**（更大更清晰）
  - padding 上下 14px → 16px；min-width 34px → 38px
  - 保留 grid + place-items:center 居中
- npm run build 通过

### v4.3 修正（用户截图反馈"标题仍不完整 + 空分类不该显示占位"）
- 用户截图显示：
  1. 标题竖排只显示 1 个字（如"运"），其余被裁剪——grid+place-items:center 在 vertical-lr 下居中没生效，min-height 200px 仍不够
  2. 空分类（天气/通航/动态等 9 个）仍显示"（暂无语音确认项）"占位文字——应该直接不渲染
- 改动：
  - 标题改用 **flex 居中**（align-items:center + justify-content:center），不用 grid（grid 在 vertical-lr 下行为不可靠）
  - min-height 200 → **260px**；字号 15 → **16px**；letter-spacing 4px 保持；min-width 38 → 42px；padding 上下 14 → 18px
  - **空分类不渲染语音语句**（sentence 为空时整块不输出），HTML 和 React 都改
- npm run build 通过

### v4.7 修正（用户反馈"类型标签不在界面展示"）
- 用户反馈："单选""多选"这种类型标签不需要在界面上展示，只是在长按出现编辑框或后端网页设置时才表示这些内容是单选/多选的
- 改动（HTML + React 同步）：
  - HTML renderComp：single/multi 去掉整个 opt-label 行（原显示"单选/多选 [类型]"）；text 字符串保留标签文字但去掉"确认"类型标签；text 数字去掉"单选"类型标签
  - opt-row 加 `data-type` 属性（single/multi/confirm）供 JS 判断；toggleChip 改用 row.getAttribute('data-type') 判断多选；updateSentence 用 data-type==='confirm' 跳过确认行
  - React 版：single/multi 去掉 label 行；text 字符串/数字去掉类型 span，保留实际标签文字
- 验证：HTML JS OK；React build 通过

### v4.8 同步（用户更新了 交接班页面.js 的 handover 数据 + chip inline-flex）
- 用户更新了 `交接班页面.js`（const handover = [...]），要求同步到两个版本：
  - **场面状况**：数字组 [0,1,2,3,4,5] 从 text 改为 **single**（数字单选）
  - **天气**：新增 comp（VIS/RVR、QNH、特殊、风向风速、特殊、复杂天气及趋势、机场警报——全是 text 标签行）
  - **空军活动**：新增（SP活动无 single；高度要求/南落北起/是否通报 multi）
  - **通航**：新增（申请中/活动中/无 single）
  - **特殊**：新增（VIP/公务机/训练/试车/非默认跑道/国际航司/无 multi）
  - **动态**：新增（进港/放行/开车/滑行/协调移交/需协调航班/相似航班号 multi）
  - 其余：运行模式/设备不变；当前进近方式/流量控制/航行通告仍空
- 用户数据 items 里含前导空格（" 活动中"、" VIP"、"  试车"等）——同步时 trim 掉
- chip 改为 **inline-flex**：`.chip` 加 `display:inline-flex; align-items:center; justify-content:center`（不要块元素）；React Chip 组件加 inline-flex items-center justify-center

### v4.9 修正（用户澄清"inline-flex 是包裹容器 + HTML 默认交接班页面"）
- 用户澄清：**不是 chip 变 inline-flex，是包裹 chip 的容器**（React 版"左侧内容"div = `.flex-1 p-3.5 pb-3 min-w-0 inline-flex`；HTML 版对应 .hg-body 加 display:inline-flex）
- React 版用户已自行改了 96 行（含 inline-flex），无需我再动
- HTML 版改动：
  - `.hg-body` 加 `display:inline-flex`
  - `currentView` 初始值 'seats' → **'handover'**（默认进入交接班页面）
- 验证：HTML JS OK；.hg-body inline-flex ✓；renderHandover 初始化调用 ✓

### 经验沉淀
- 用户给代码示例时（如 React className），意味着"HTML 版要对应成这样"——先检查 React 版是否用户已改，避免重复修改
- 用户说"默认交接班页面"= currentView 初始值改成 handover

### v4.10 修正（用户反馈"不要再有确认无误/不符"）
- text 字符串类型（西跑道道面状况代码/VIS/RVR/QNH/特殊/风向风速/复杂天气及趋势/机场警报/西跑道+A滑行）原来渲染"确认无误/不符"两个 chips，全部去掉
- 现在 text 字符串项只渲染为**纯标签提示行**（淡灰小字，无任何按钮/chips）
- HTML renderComp：data-type='confirm' 分支改为 data-type='text' 只输出 opt-label
- updateSentence 跳过 data-type='text' 行（无选项，不参与语音语句）
- React 版：text 字符串分支去掉确认无误/不符 chips，只保留标签 span；toggleConfirm 成为死函数（保留无害）
- 验证：HTML JS OK；grep 确认"确认无误""不符"已完全移除；React build 通过

### 待确认
- 错误判断逻辑（和预设值对比？还是仅靠语音识别？）
- 录音实现方式（Web Speech API？原生？）
- text 类型"西跑道道面状况代码" 与 数字 0-5 是否要合并为一行
- 语音语句是否每个大分类预设一句（还是纯动态拼接）
- 验证：HTML JS OK；React build 通过

### 经验沉淀
- 用户会持续更新数据源文件（交接班页面.js），同步时注意：类型变化（text→single）、新增 comp、items 前导空格要 trim
- chip 类组件用 inline-flex + align/justify center，避免块级拉伸

### 经验沉淀
- **writing-mode + grid 的居中在 Safari/iPad 上不可靠**：优先用 `display:flex + align-items:center + justify-content:center`，flex 在 writing-mode 下行为稳定
- 竖排标题居中最稳组合：writing-mode:vertical-lr + display:flex + align-items:center + justify-content:center + min-height 足够大
- 空状态占位文字要看用户意图：有时候用户希望"没内容就什么都不显示"而非"显示暂无占位"

### v4.4 紧急修复（页面空白！）
- 用户反馈"怎么什么内容都没有了"——页面完全空白
- 根因：v4.3 用 Edit 修改 renderHandover 时，**old_string 匹配到旧代码段，新代码被追加成重复段**——`const parts` / `const sentence` 重复声明 → JS 语法错误 → 整个 <script> 崩溃 → 所有视图都不渲染
- 修复：删除重复的旧声明段（`const sentence = parts.length ? '现在是' + parts.join('，') : '（暂无语音确认项）';` + 重复的 parts/sentence 声明），保留新的 voiceBlock 条件渲染逻辑
- 验证：用 node `new Function(script)` 检查 JS 语法 OK；grep 确认无重复声明；关键元素（handoverBody/HANDOVER_GROUPS/renderHandover/hg-title/watermark/floats）全部存在
- **教训：Edit 的 old_string 若匹配到文件中不存在的精确文本，会替换失败——但本次是 old_string 与实际内容部分匹配导致插入重复。以后修改大段代码前先 Read 确认精确内容，改完立即用 node 校验 JS 语法**

### v4.5 修正（用户反馈"HandoverGroup 不能正常被内容撑开"）
- 根因（两个版本都有）：
  - **HTML 版**：`.hg` 是 v2-body（flex column 滚动容器）的 flex item，**flex 默认 flex-shrink:1** → 内容高的卡片被父容器压缩，超出部分被 .hg 的 overflow:hidden 裁剪 → 卡片不跟随内容高度
  - **React 版**：用户自己改的容器加了 `h-full` + `overflow-auto`——`h-full` 让每个卡片强制占满容器可视高度，高度不再由内容决定；另外用户把 comp 渲染 JSX 注释掉了（卡片内无内容）
- 修复：
  - HTML 版 `.hg` 加 `flex-shrink:0`（高度由内容决定，超出自然后滚）
  - React 版 HandoverGroup 容器：去掉 `h-full`、`overflow-auto` 改回 `overflow-hidden`、加 `shrink-0`
  - **React 版 comp 渲染注释未动**（用户自己注释的，可能在实验，留给用户决定）
- 验证：HTML JS 语法 OK；React build 通过
- **提醒用户**：React 版注释掉的 comp 渲染段如果不需要注释，需要自行恢复

### 经验沉淀
- **flex column 滚动容器里的卡片必须 flex-shrink:0**：否则内容高的卡片被压缩裁剪，表现为"卡片不能被内容撑开"
- h-full 会让 flex item 占满容器高度——如果想让高度跟随内容，绝对不能加 h-full
- 用户会在 React 版自行改代码（他说 UI 交互他自己写），改前先读文件确认当前状态，避免覆盖用户实验中的改动

### v4.6 修正（用户反馈"不要最低高度，由内容撑开"）
- 用户反馈：不要写死 min-height:260px——"航行通告"和"动态"标题文本长度不一样，但大项高度一样，说明被写死高度约束了
- 改动（HTML + React 同步）：**删除 .hg-title 的 min-height:260px**，只保留 flex 居中（align-items:center + justify-content:center）+ vertical-lr
- 现在卡片高度完全由内容决定：.hg 高度 = max(标题自然竖排高度, hg-body 内容高度)，flex-shrink:0 保证不被父级压缩
- 效果："动态"2 字卡片矮、"航行通告"4 字卡片较高、"运行模式"选项多卡片最高——高度各不相同，由内容撑开
- 标题仍完整展示：flex 布局中标题自然高度参与计算，不会被裁剪
- npm run build 通过

### 经验沉淀
- 用户说"由内容撑开"时，坚决不要设任何 min-height——即使之前为了"标题完整展示"设了最低高度，用户要的是自然高度
- 竖排标题完整展示的正确做法：flex-shrink:0（不被压缩）+ 不设 min-height，让标题自然高度参与 flex 布局
- 用户通过"两个标题长度不同的卡片高度却一样"来判断是否写死——这是很好的测试用例

### 待确认
- 错误判断逻辑（和预设值对比？还是仅靠语音识别？）
- 录音实现方式（Web Speech API？原生？）
- text 类型"西跑道道面状况代码" 与 数字 0-5 是否要合并为一行
- 语音语句是否每个大分类预设一句（还是纯动态拼接）
